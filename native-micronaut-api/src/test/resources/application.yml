datasources:
  default:
    url: jdbc:tc:postgresql:14.1:///postgres
    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver
jpa:
  default:
    entity-scan:
      packages: 'com.xavierbouclet.nativeapi'
    properties:
      hibernate:
        hbm2ddl:
          auto: update
